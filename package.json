{
  "name": "dconsole.js",
  "version": "0.1.2",
  "main": "dist/console.js",
  "license": "Unlicense",

  "repository" :
  { 
    "type" : "git",
    "url" : "https://github.com/danielskapunk/dconsole.git"
  },
  
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },

  "scripts": {
  	"build": "babel src --presets env --out-dir dist"
  },

  "babel": {
    "presets": [
      "env"
    ]
  }
}
